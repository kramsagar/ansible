Ansible important files ->
1. ansible.cfg
    Priority order:
      1. Ansible look for env variable defined file
      2. user home directory
      3. current working directory
      4. /etc/ansible/ansible.cfg
    
2. hosts inventory
      There are two types of inventories: by default inventory from ansible.cfg or else customized hosts list.

3. modules: These are python programs
     
